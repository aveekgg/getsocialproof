{
  "name": "roomreel-challenge",
  "version": "1.0.0",
  "description": "Student video challenge app",
  "scripts": {
    "build": "npm run build --workspace=client",
    "dev": "npm run dev --workspace=server",
    "start": "npm run start --workspace=server"
  },
  "workspaces": [
    "client",
    "server",
    "shared"
  ]
}